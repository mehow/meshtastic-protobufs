# Meshtastic Protobuf Definitions<br>with servo support

## Overview

This is a fork of the official Protobuf message definitions for the Meshtastic project, adding `ServoControlConfig` message to `ModuleConfig`.
